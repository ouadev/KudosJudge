+ threding isolated (tested till 10 workers)							OK
+ template communication, signal sending data exchange					OK
		+ send signal and quit (agressive)								OK
		+ data exchange (without cloning), serialization				OK
			+ full exchange (shared memory, except some hanging)   		OK (hangs, may be because signal handling, should mask)
+ template launching the Watcher										IN PROGRESS
		+ watcher exits after launch 									OK (fix THRADS_MAX bug)
		+ ERFS-mode enable/disable										OK
+
+ sandbox executing programs in threaded-mode							OK